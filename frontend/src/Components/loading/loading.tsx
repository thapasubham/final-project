import { CircularProgress, Container } from "@mui/material";

function Loading() {
    return (
        <Container>
            < CircularProgress />
        </Container>
    );
}

export default Loading;