
services:

  database:
    image: postgres
    ports: 
      - 8000:5432
    environment:
      POSTGRES_PASSWORD: subham12
    volumes:
      - db:/var/lib/postgresql/data

  # backend:
  #   build: /backend
  #   ports: 
  #   - 3000:3000
  #   volumes:
  #   - type: bind
  #     source: ./backend/src
  #     target: /app/src
  #   command: npm run dev 
  frontend:
    restart: unless-stopped
    # depends_on:
    #   - backend
    build: /frontend
    ports: 
    - 5000:5000
    command: npm run dev 
    volumes:
    - type: bind
      source: ./frontend/src
      target: /app/src

volumes:
    db:
  